{
  "routes": [
    {
      "method": "GET",
      "path": "/pants",
      "handler": "pants.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pants/count",
      "handler": "pants.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pants/:id",
      "handler": "pants.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pants",
      "handler": "pants.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pants/:id",
      "handler": "pants.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pants/:id",
      "handler": "pants.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
